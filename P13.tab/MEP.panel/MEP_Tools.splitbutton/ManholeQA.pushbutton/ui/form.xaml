<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Manhole QA" Height="680" Width="1100"
    WindowStartupLocation="CenterScreen"
    Background="#F5F5F5" FontFamily="Segoe UI" FontSize="13">

    <Window.Resources>
        <!-- Button base -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status cell color converter via tag -->
        <Style x:Key="CellOk" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>
        <Style x:Key="CellChanged" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E65100"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="CellWarn" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#B71C1C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="16">

        <!-- ── TOOLBAR ── -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
                    Margin="0,0,0,12" Height="36">
            <TextBlock Text="Workset" VerticalAlignment="Center" Margin="0,0,6,0"
                       Foreground="#666"/>
            <ComboBox x:Name="CbWorkset" Width="160" Height="30"
                      VerticalContentAlignment="Center"/>
            <TextBlock Text="สถานะ" VerticalAlignment="Center" Margin="12,0,6,0"
                       Foreground="#666"/>
            <ComboBox x:Name="CbStatus" Width="140" Height="30"
                      VerticalContentAlignment="Center">
                <ComboBoxItem Content="ทั้งหมด" IsSelected="True"/>
                <ComboBoxItem Content="มีการเปลี่ยนแปลง"/>
                <ComboBoxItem Content="ต้องตรวจสอบ"/>
                <ComboBoxItem Content="ไม่มีการเปลี่ยนแปลง"/>
            </ComboBox>
            <Button x:Name="BtnScan" Content="⟳  Scan view"
                    Margin="12,0,0,0" Width="110"/>
            <CheckBox x:Name="ChkDryRun" Content="Preview only"
                      IsChecked="True" VerticalAlignment="Center"
                      Margin="20,0,0,0" Foreground="#444"/>
            <Button x:Name="BtnCommit" Content="Commit ที่เลือก"
                    Margin="10,0,0,0" Width="120"
                    Background="#FFEBEE" BorderBrush="#EF9A9A"/>
            <Button x:Name="BtnExport" Content="Export CSV"
                    Margin="8,0,0,0" Width="100"/>
        </StackPanel>

        <!-- ── STAT BAR ── -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/><ColumnDefinition/>
                <ColumnDefinition/><ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="บ่อทั้งหมด" FontSize="11" Foreground="#888"/>
                    <TextBlock x:Name="StatTotal" Text="—" FontSize="22" FontWeight="Medium"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="ไม่มีการเปลี่ยนแปลง" FontSize="11" Foreground="#888"/>
                    <TextBlock x:Name="StatOk" Text="—" FontSize="22"
                               FontWeight="Medium" Foreground="#2E7D32"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="มีการเปลี่ยนแปลง" FontSize="11" Foreground="#888"/>
                    <TextBlock x:Name="StatChanged" Text="—" FontSize="22"
                               FontWeight="Medium" Foreground="#E65100"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6" Padding="12,8">
                <StackPanel>
                    <TextBlock Text="ต้องตรวจสอบ" FontSize="11" Foreground="#888"/>
                    <TextBlock x:Name="StatWarn" Text="—" FontSize="22"
                               FontWeight="Medium" Foreground="#B71C1C"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ── STATUS BAR ── -->
        <Border DockPanel.Dock="Bottom" Background="White"
                BorderBrush="#E0E0E0" BorderThickness="0,1,0,0"
                Padding="0,8,0,0" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" x:Name="LblStatus" Content="กด Scan view เพื่อเริ่มต้น"
                       Foreground="#888" FontSize="12"/>
                <ProgressBar Grid.Column="1" x:Name="ScanProgress" 
                             Minimum="0" Maximum="100" Height="14" 
                             Margin="0,0,16,8" Visibility="Hidden"
                             Foreground="#2E7D32"/>
            </Grid>
        </Border>

        <!-- ── MAIN SPLIT ── -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition x:Name="DetailRow" Height="0"/>
            </Grid.RowDefinitions>

            <!-- TABLE -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6">
                <DataGrid x:Name="ListRecords"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          Background="White" BorderThickness="0"
                          RowBackground="White"
                          AlternatingRowBackground="#FAFAFA"
                          HorizontalGridLinesBrush="#F0F0F0"
                          FontSize="12">
                    <DataGrid.Columns>

                        <!-- Checkbox -->
                        <DataGridTemplateColumn Header="" Width="32">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding checked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- ID -->
                        <DataGridTextColumn Header="ID" Binding="{Binding id}"
                                            Width="90" IsReadOnly="True"/>

                        <!-- Workset -->
                        <DataGridTextColumn Header="Workset" Binding="{Binding ws}"
                                            Width="130" IsReadOnly="True"/>

                        <!-- Status badge -->
                        <DataGridTemplateColumn Header="สถานะ" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="6,2"
                                            HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding status}" Value="ok">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding status}" Value="changed">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding status}" Value="warn">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding status_label}" FontSize="11">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding status}" Value="ok">
                                                            <Setter Property="Foreground" Value="#2E7D32"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding status}" Value="changed">
                                                            <Setter Property="Foreground" Value="#E65100"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding status}" Value="warn">
                                                            <Setter Property="Foreground" Value="#B71C1C"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Main C1-C4 -->
                        <DataGridTemplateColumn Header="C1 (main)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding c1m}" HorizontalAlignment="Center"
                                               Foreground="{Binding c1m_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="C2 (main)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding c2m}" HorizontalAlignment="Center"
                                               Foreground="{Binding c2m_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="C3 (main)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding c3m}" HorizontalAlignment="Center"
                                               Foreground="{Binding c3m_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="C4 (main)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding c4m}" HorizontalAlignment="Center"
                                               Foreground="{Binding c4m_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Extra E1-E4 -->
                        <DataGridTemplateColumn Header="E1 (extra)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding e1}" HorizontalAlignment="Center"
                                               Foreground="{Binding e1_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="E2 (extra)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding e2}" HorizontalAlignment="Center"
                                               Foreground="{Binding e2_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="E3 (extra)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding e3}" HorizontalAlignment="Center"
                                               Foreground="{Binding e3_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="E4 (extra)" Width="72">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding e4}" HorizontalAlignment="Center"
                                               Foreground="{Binding e4_color}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Isolate button -->
                        <DataGridTemplateColumn Header="" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🔍" Width="32" Height="24"
                                            FontSize="13" Tag="{Binding id}"
                                            Click="OnIsolateClick"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- SPLITTER -->
            <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch"
                          Background="Transparent" Cursor="SizeNS"
                          ResizeDirection="Rows"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- DETAIL PANEL -->
            <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0"
                    BorderThickness="1" CornerRadius="6" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <TextBlock Text="MAIN CONNECTION" FontSize="11"
                                   Foreground="#888" FontWeight="Medium"
                                   Margin="0,0,0,8"/>
                        <ItemsControl x:Name="DetailMain">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding label}"
                                                   Foreground="#666"/>
                                        <TextBlock Grid.Column="1" Text="{Binding old_val}"
                                                   Foreground="#AAAAAA"
                                                   TextDecorations="{Binding strike}"/>
                                        <TextBlock Grid.Column="2" Text="→"
                                                   Foreground="#CCC"
                                                   HorizontalAlignment="Center"
                                                   Visibility="{Binding arrow_vis}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding new_val}"
                                                   Foreground="{Binding val_color}"
                                                   FontWeight="{Binding val_weight}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="EXTRA CONNECTION" FontSize="11"
                                   Foreground="#888" FontWeight="Medium"
                                   Margin="0,0,0,8"/>
                        <ItemsControl x:Name="DetailExtra">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="80"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding label}"
                                                   Foreground="#666"/>
                                        <TextBlock Grid.Column="1" Text="{Binding old_val}"
                                                   Foreground="#AAAAAA"
                                                   TextDecorations="{Binding strike}"/>
                                        <TextBlock Grid.Column="2" Text="→"
                                                   Foreground="#CCC"
                                                   HorizontalAlignment="Center"
                                                   Visibility="{Binding arrow_vis}"/>
                                        <TextBlock Grid.Column="3" Text="{Binding new_val}"
                                                   Foreground="{Binding val_color}"
                                                   FontWeight="{Binding val_weight}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </DockPanel>
</Window>