[project]
name = "p13-revit-mcp"
version = "0.3.0"
description = "Secured MCP bridge for P13 pyRevit automation"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "mcp[cli]>=1.10.0",
]

[tool.uv]
package = false
